# Gemini Router

**Token Discipline:** Keep this file short. Read the imported files directly instead of copying them here.

@AGENTS.md
@docs/PROJECT.md
@docs/CONVENTIONS.md
Use `docs/*` as the durable memory layer for ongoing work.
@docs/TODO.md
@docs/DECISIONS.md
@docs/STATE.md

`AGENTS.md` owns the mandatory policy; use this router only to reach the deeper context.

**Before editing:** Fill obvious placeholders in `docs/PROJECT.md` or `docs/CONVENTIONS.md` with concise factual content.

**After editing:** If commands, conventions, or decisions changed, update the matching docs in the same PR. Update `docs/STATE.md` only when priorities or handoff state changed.

Do not duplicate policy text here. Use this file purely as an index to the deeper context in `docs/`.
